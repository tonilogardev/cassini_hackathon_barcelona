[tool.poetry]
name = "antigravity-geo-ml"
version = "0.1.0"
description = "Geospatial-AI CLI for satellite imagery inference"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.10"
typer = "^0.9.0"
torchgeo = "^0.5.0"
rasterio = "^1.3.9"
torch = "^2.2.0"
torchvision = "^0.17.0"
torchaudio = "^2.2.0"
geopandas = "^0.14.3"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
